apiVersion: v1
kind: Service
metadata:
  name: ui-loadbalancer
  namespace: practise
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  type: LoadBalancer
  selector:
    app: ui
  ports:
    - port: 80
      targetPort: 80
